<!DOCTYPE html>
<html>
  <head>
    <title>Exam Preparation Furnace</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
    <%= favicon_link_tag 'logo-uet.ico' %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body class="index-page sidebar-wrapper">
    <%= render partial: "shared_example/headers" %>
    <%= custom_bootstrap_flash %>
    <div class="main main-raised mt-0">
      <div class="row mx-0">
        <nav class="sidebar col-md-2">
          <nav class="nav nav-pills flex-column">
            <%= link_to "#", class: "btn btn-primary btn-round" do %>
              demo1
            <% end %>
            <%= link_to "#", class: "btn btn-round" do %>
              demo2
            <% end %>
            <%= link_to "#", class: "btn btn-round" do %>
              demo3
            <% end %>
          </nav>
        </nav>
        <div class="col border-left-1 border-light">
          <%= yield %>
        </div>
      </div>
    </div>
  </body>
</html>
